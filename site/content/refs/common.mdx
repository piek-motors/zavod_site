import { CodeBlock, Pre } from 'fumadocs-ui/components/codeblock'

export function Resp(props) {
  return (
    <CodeBlock title="Response example" allowCopy>
      <Pre>
        {JSON.stringify(props.json, null, 2)}
      </Pre>
    </CodeBlock>
  )
}

export function CurlReq(props) {
  return (
    <CodeBlock title="Curl example" allowCopy>
      <Pre>{props.children}</Pre>
    </CodeBlock>
  )
}
